import { h } from "preact";

function LimerickPage() {
  return (
    <div>
      <p>There once was a tiny app,</p>
      <p>which wouldnt do much, but ZAP! It would load</p>
      <p>
        in a second, so fast I reckon; it might deserve a bit of a clap 👏 👏
      </p>
    </div>
  );
}
export default LimerickPage;
